[Unit]
Description=Dropbox daemon
After=network.target syslog.target

[Service]
Type=simple
User={{ system.user }}
ExecStart={{ dropbox.daemon_directory }}{{ dropbox.daemon_name }}
ExecStop=/bin/kill -HUP $MAINPID
Restart=always
RestartSec=1

[Install]
WantedBy=multi-user.target